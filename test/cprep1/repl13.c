
#define ZERO_ARRAY_1(a, n) for (int i = 0; i < (n); i++)        \
       (a)[i]=0;
#define ZERO_ARRAY_2(a, n)                      \
   for (int i = 0; i < (n); i++)                \
       (a)[i]=0;
#define ZERO_ARRAY_3(a, n) for (int i = 0; i < (n); i++)        \
       (a)[i]=0;


